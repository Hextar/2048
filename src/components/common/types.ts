export enum UiVariant {
  FILLED = 'filled',
  OUTLINE = 'outline',
}

export enum UiColor {
  PRIMARY = 'primary',
}

export enum UiSize {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large',
}

export enum UiFlair {
  SOFT = 'soft',
  ROUNDED = 'rounded',
  SQUARED = 'squared',
}

export enum UiTag {
  BUTTON = 'button',
  ROUTER = 'router-link',
  A = 'a',
  DIV = 'div',
}